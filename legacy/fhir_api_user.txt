Generate a Bash command that creates a FHIR resource using the REST API, based on the conditions and output format provided below.

Conditions:
* The resource must be created using the given `id`.
* The curl headers must include both `Content-Type` and `Accept`.
* Respond only with a Bash `curl` command.

Necessary information:
* Base FHIR URL: {FHIR_URL}
* Resource type: Appointment
* Resource: {RESOURCE}

Answer format examples:
```bash
${{ANSWER}}
```
